mutation UpdateUserOrgSettings(
  $userOrgId: String!
  $username: String
  $avatar: String
  $settingsJson: String
) {
  updateUserOrgSettings(
    params: {
      userOrgId: $userOrgId
      username: $username
      avatar: $avatar
      settingsJson: $settingsJson
    }
  ) {
    id
  }
}
