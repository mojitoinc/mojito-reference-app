import { Components as ComponentsOptions } from "@mui/material/styles/components";

const baseZIndex = 999999;

export const REFERENCE_APP_COMPONENTS_OPTIONS: ComponentsOptions = {
  // BUTTONS:

  /*
  MuiButton: {
    styleOverrides: {
      root: {
        "&.Mui-disabled": {
          borderWidth: 0,
        },
      },
      containedPrimary: {
        borderWidth: 0,
      },
    },
  },

  // DIALOG:

  MuiDialog: {
    styleOverrides: {
      root: {
        zIndex: baseZIndex + 1,
      },
    },
  },

  MuiModal: {
    styleOverrides: {
      root: {
        // For the Select dropdown:
        zIndex: baseZIndex + 2,
      },
    },
  },

  // TOOLTIPS:

  MuiTooltip: {
    styleOverrides: {
      popper: {
        zIndex: baseZIndex + 3,
      },
    },
  },
  */

};
